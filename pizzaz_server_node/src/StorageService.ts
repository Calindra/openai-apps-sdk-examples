export default class StorageService {
  static async getStorageItem(key: string): Promise<any> {
    // Placeholder for now
    return Promise.resolve(null);
  }

  static async setStorageItem(key: string, value: any): Promise<void> {
    // Placeholder for now
    return Promise.resolve();
  }

  static async removeItem(key: string): Promise<void> {
    // Placeholder for now
    return Promise.resolve();
  }

  static async getStorageJSON(key: string): Promise<any> {
    // Placeholder for now
    return Promise.resolve(null);
  }

  static async setStorageJSON(key: string, value: any): Promise<void> {
    // Placeholder for now
    return Promise.resolve();
  }
}